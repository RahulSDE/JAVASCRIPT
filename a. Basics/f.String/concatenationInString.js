/*
    ************* + operator in a string ****************
    > Adds two string, if operand is not a string, then it convert the operand into string and then add it.
    > String is immutable. You cannot update the value present at particular index.
*/

console.log("" + 1432);
let x = "" + 1432;
console.log(typeof x);